To use this program, 
    first create a file named quiz_database.sqlite
    then run populate_test_data using python3+ (DOING SO DROPS ALL TABLES from quiz_database.sqlite, and repopulates the question table with default question data)
    then run main

For testing,
    replace the database_path variable in database_config.py to the test database of your choice